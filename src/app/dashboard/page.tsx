import { SurfacePlaceholder } from '@/components/layout/SurfacePlaceholder';

export default function DashboardPlaceholder() {
  return (
    <SurfacePlaceholder
      surfaceKey="Surface B · User Dashboard"
      title="Bireysel kontrol paneli"
      subtitle="KPI'lar, ilanlarım, AI bugün — Phase 3'te eklenecek (PROMPT §7.B.B1)."
      accent="lime"
    />
  );
}
