import { SurfacePlaceholder } from '@/components/layout/SurfacePlaceholder';

export default function AgentOverviewPlaceholder() {
  return (
    <SurfacePlaceholder
      surfaceKey="Surface D · Agent / MCP"
      title="Agent Operations"
      subtitle="MCP transport, tool registry, trace explorer — Phase 6'da eklenecek (PROMPT §7.D.D1)."
      accent="magenta"
    />
  );
}
